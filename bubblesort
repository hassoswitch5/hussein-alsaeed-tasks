def bubblesort(list):
    for j in range (len(list)):
        for i in range (0, len(list)-1):
            if list[i] > list[i+1]:
                list[i],list[i+1] = list[i+1],list[i]
            
    return list
            





list = [4,7,9,1,3,6,10]


bubblesort(list)
print(list)
